prepare:
  - npm install
graders:
  - name: smoke-test
    run: node -e "const s = require('./server/index.js'); setTimeout(() => process.exit(0), 500)"
  - name: unit-tests
    run: node --test test/similarity.test.js
